# กฎของโปรเจกต์ ECHO

- **คุยกับผู้ใช้เป็นภาษาไทยเสมอ** (ข้อความตอบ สรุปงาน คำถาม) — โค้ด ชื่อตัวแปร และ commit message ใช้ตามคอนเวนชันเดิมของ repo
- **ไม่ทำหน้าจอมือถือแล้ว** — ไม่ต้องออกแบบ แก้ หรือทดสอบ layout มือถือ (`vp.w < 768` ใน `client/src/screens/Game.jsx` และ `MobileOpponent`)
  งานใหม่ทำเฉพาะจอคอม/แท็บเล็ตเท่านั้น โค้ดมือถือที่มีอยู่ปล่อยไว้ตามเดิม ไม่ต้องลบ
- ข้อความที่ผู้เล่นเห็นในเกมใช้ภาษาไทย คำที่ไม่มีคำไทยให้ทับศัพท์ (เช่น "คริติคอล", "สวนกลับ") ยกเว้นชื่อเฉพาะ
- เอกสารระบบเกมอยู่ที่ [GAME_SYSTEM.md](GAME_SYSTEM.md) — อ่านก่อนแก้ engine

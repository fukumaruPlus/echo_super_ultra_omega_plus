# SE.RA.PH Moon Cell — สเปกฉาก · อนิเมชัน · การแจ้งเตือน

> เอกสารออกแบบงานภาพ/เสียง (presentation spec) — **ยังไม่ได้ลงมือเขียนโค้ด**
> กติกาและตัวเลขอยู่ใน [SERAPH_MOONCELL.md](SERAPH_MOONCELL.md) · เอนจินอยู่ใน [GAME_SYSTEM.md](GAME_SYSTEM.md)
> เอกสารนี้ตอบคำถามเดียว: **ผู้เล่นเห็นอะไร ได้ยินอะไร ในทุกจังหวะของโหมดนี้**

**อ้างอิงอารมณ์:** Persona 5 (จังหวะการตัดฉาก · แถบทแยง · ปฏิทินวัน · เมนูปลายทาง) + Fate/EXTRA (SE.RA.PH เป็นทะเลข้อมูล · ทุกอย่างคือดาต้า · ตัวตนของศัตรูคือความลับที่ต้องขุด · ผู้แพ้ถูก "ลบ" ไม่ใช่ "ตาย")

---

## 1. หลักการออกแบบ 5 ข้อ

| # | หลักการ | เหตุผล |
|---|---|---|
| 1 | **ม่วง = ตัวตนผู้เล่น · ฟ้าไซแอน = ระบบ Moon Cell** | โหมดนี้มีสองเสียงเล่าเรื่อง ต้องแยกออกจากกันด้วยสีทันทีโดยไม่ต้องอ่าน |
| 2 | **ทุกการกระทำมีเสียงและมีการเคลื่อนไหว ไม่มีอะไรเปลี่ยนค่าเงียบ ๆ** | ค่าที่ขยับในโหมดนี้ (Matrix / ความจุ / ระดับทักษะ) ไม่มีเฟสโจมตีคอยย้ำ ต้องดังด้วยตัวมันเอง |
| 3 | **ความลับต้องมีรูปร่างที่มองเห็นได้** | "ไม่รู้ว่าใครเล่นตัวอะไร" ต้องเป็นเงาดำ + `???` ที่เห็นตลอด 4 วัน ไม่ใช่แค่ไม่มีข้อมูล |
| 4 | **ฉากซ้ำต้องสั้นลง ฉากที่เกิดครั้งเดียวถึงจะยาวได้** | 1 รอบมี 5 วัน เล่นวนหลายรอบ — ฉากเปิดวันยาว 2.4 วิ × 5 × หลายรอบคือความทรมาน |
| 5 | **วันที่ 1–4 กับวันที่ 5 ต้องเหมือนเป็นคนละเกม** | เป็นแก่นของสเปก: 4 วันแรกเจ็บไม่ได้เลย วันที่ 5 ตายได้ — งานภาพต้องขายความต่างนี้ให้สุด |

---

## 2. ภาษาภาพ — เลเยอร์ SE.RA.PH ทับบน `p-*` เดิม

**สิ่งที่ใช้ของเดิมทั้งดุ้น** (ห้ามสร้างใหม่ทับซ้อน)

| ของเดิม | ใช้ที่ไหนในโหมดนี้ |
|---|---|
| `TransitionCurtain` (`preTrigger` / `holdCover` / `release`) | สลับระหว่างสนาม ↔ หน้าเลือกสถานที่ ↔ ฉากในสถานที่ |
| `.p-btn-cut` · `.p-chip` · `.p-panel` · `.p-slash-btn` · `.p-scroll` | ปุ่ม/ป้าย/แผงทั้งหมดของโหมด |
| `.p-target-ring` + `.p-target-corner` | ตัวล็อกเป้า Matrix ที่สวนสาธารณะ (คนละบริบท กลไกภาพเดียวกัน) |
| `.p-name-tag` · `.p-player-frame` | การ์ดผู้เล่นในสนาม (เปลี่ยนแค่เนื้อใน) |
| `.p-scan-hover` · `.p-rise` · `.glitch-p` · `.p-flicker` | การ์ดสถานที่ · การ์ดสกิลที่ปลดล็อก · หัวข้อระบบ |
| `runCutsceneQueue()` + `onlyFor` (patch 2.8.1) | ฉากระดับ T3 ทั้งหมด — **`onlyFor` คือกุญแจของฉากเปิดเผยตัวละคร** |
| `useCardFlights` / `FlyingCardsLayer` | ของที่ "บินจากจุดหนึ่งไปอีกจุด" ทุกชิ้น (Matrix, เหรียญ, ไอเทม) |
| `framer-motion` | ฉากที่ต้อง orchestrate หลายชั้น (ใช้แบบเดียวกับ `CycleBanner`) |

**โทเคนใหม่** (เพิ่มใน `@theme` ของ [client/src/index.css](client/src/index.css) ไม่แตะของเดิม)

```css
--color-sc-cyan:  #35e6d4;  /* เสียงของระบบ Moon Cell — เส้นตาราง ข้อความระบบ Matrix */
--color-sc-mint:  #7cf59b;  /* ยืนยันสำเร็จ / ค่าที่เพิ่มขึ้น */
--color-sc-red:   #ff4d5e;  /* การคัดออก · คู่ดวล · "ถูกจับตา" */
--color-sc-void:  #04070c;  /* ดำอมน้ำเงิน พื้นของ SE.RA.PH */
--color-sc-line:  rgba(53, 230, 212, 0.35);
--font-sc-mono:   "JetBrains Mono", "Chakra Petch", ui-monospace, monospace;
```

**องค์ประกอบภาพประจำโหมด 4 ตัว**

1. **ตารางเส้น (grid)** — เส้นบาง `--color-sc-line` วิ่งเป็นตารางเปอร์สเปกทีฟ เลื่อนช้า ๆ ตลอดเวลา เป็นพื้นของทุกฉาก
   3 ระดับ: `.sc-grid-faint` (ในสนาม บางมากไม่กวนสายตา) · `.sc-grid` (ฉากเต็มจอ) · `.sc-grid-hot` (ฉากคัดออก — เส้นแดง สั่น)
2. **กลิตช์ตัด (glitch cut)** — คู่กับ `sond_effect/cut_glit.mp3` เสมอ ห้ามใช้แยกกัน: เลื่อนสี RGB แยกช่อง 3 เฟรม + สไลซ์แนวนอน 6 แถบ 180ms
   > **กฎ:** กลิตช์ = "ระบบเพิ่งเขียนความจริงใหม่ทับ" ใช้เฉพาะตอนสถานะโลกเปลี่ยนจริง (เปิดเผยตัวตน · ปลดล็อกสกิล · เข้าวันที่ 5 · ลบผู้เล่น) **ห้ามใช้เป็นเครื่องประดับ**
3. **ข้อความระบบ (system line)** — `--font-sc-mono` สีไซแอน ขึ้นต้นด้วย `>` พิมพ์ทีละตัวอักษร 18ms/ตัว เป็นเสียงของ Moon Cell ที่พูดกับผู้เล่น (ภาษาอังกฤษล้วน — ให้ต่างจากข้อความเกมที่เป็นไทย)
4. **เศษข้อมูล (data shard)** — ต่อยอด `.p-shard` เดิม แต่เป็นสี่เหลี่ยมข้อมูลไซแอน ใช้ตอนอะไรประกอบร่างหรือสลาย

**กฎการแยกวันที่ 1–4 ออกจากวันที่ 5**

| | วันที่ 1–4 | วันที่ 5 |
|---|---|---|
| พื้นหลัง | ตารางไซแอนเย็น เคลื่อนช้า สงบ | ตารางแดง/ส้ม สั่นตามจังหวะเพลง |
| หลอดเลือด/เกราะ | **ซ่อนทั้งกระดาน** (ไม่มีดาเมจ — ไม่มีอะไรให้ดู) | **โผล่กลับมาพร้อมกันทุกคนด้วยอนิเมชัน** (ดู S7) |
| การ์ดผู้เล่นอื่น | เงาดำ + `???` | คู่ที่ลงสนาม = ภาพจริง · คนอื่นยังเงาดำ |
| ความเร็วฉาก | นุ่ม `cubic-bezier(.2,.8,.2,1)` | กระแทก `cubic-bezier(.6,0,.3,1)` + แฟลช |

---

## 3. แผนที่ฉาก + งบเวลา

```
[เริ่มแมตช์]
  S0  บูตระบบ SE.RA.PH ......................... 4.2s   ครั้งเดียวตอนเริ่มแมตช์
   │
   ├─ S1  แบนเนอร์เปิดวัน (DAY n) .............. 2.4s → 1.4s ตั้งแต่วันที่ 2
   │  S2  เฟสจั่วไพ่ ........................... ตาม CARD_TIME
   │  S3  เปิดไพ่ + ผู้ชนะ + รางวัล ............ 4.0s
   │  S4  เลือกสถานที่ ......................... 20s (นับถอยหลัง)
   │  S5  ฉากในสถานที่ ......................... 2.8–5.0s
   │      └ S6 ประกาศคู่ดวล (เฉพาะจบวันที่ 2) .. 7.0s   ครั้งเดียวต่อรอบ
   │  (วน 4 วัน)
   │
   ├─ S7  เข้าวันที่ 5 ......................... 3.6s   ครั้งเดียวต่อรอบ
   │  S8  เรียกคู่ + เปิดเผยตัวละคร ............ 6.4s / คู่
   │  S9  ดวล ................................. ตามเกม
   │  S10 ตกรอบ ............................... 4.0s / คน
   │  (วนจนหมดคิว)
   │
   └─ S11 จบรอบ + รางวัล ...................... 3.2s
[วนรอบใหม่ — สลับกลางวัน/กลางคืนทั้งรอบ]

S12 ผู้ชนะคนสุดท้าย ............................ 8.0s
```

**งบเวลาต่อ 1 วันธรรมดา ≈ 8 วินาทีที่ไม่ใช่การเล่น** (S1 1.4 + S3 4.0 + S5 ~2.8) — รับได้ แต่มีกฎกันบวม 3 ข้อ:

1. **ฉากซ้ำครั้งที่ 2 เป็นต้นไปย่อลง** — ใช้แนวคิด `p.cutsceneShown` เดิม: S1 ตัดจาก 2.4 → 1.4s · S5 ของสถานที่เดิมตัด build-up ทิ้ง
2. **โหมดประหยัด (`lowQ`) ใช้ได้ทุกฉาก T3** — ข้ามภาพ แต่ **ยังต้องรอเวลาเท่ากัน** (กฎเดิมของเอนจิน ห้ามแหก ไม่งั้นหลุดซิงก์)
3. **ฉากในสถานที่เล่นพร้อมกันทุกคน** ไม่ใช่ผลัดกันทีละคน — ไม่งั้น 7 คน × 3 วิ = 21 วิต่อวัน

---

## 4. ฉากทั้งหมด

### S0 — บูตระบบ SE.RA.PH (แทน `GameIntro` เฉพาะโหมดนี้) · 4.2s

> ทำไมต้องแทน: `GameIntro` เดิมเผยหน้า+ชื่อตัวละครทุกคน ซึ่ง **ทำลายแก่นของโหมดนี้ทันทีตั้งแต่วินาทีแรก**

| เวลา | ภาพ | เสียง |
|---|---|---|
| 0ms | จอดำสนิท `--color-sc-void` | เสียงหึ่งต่ำ (ambient) |
| 0–600 | เส้นตารางวาดตัวเองจากจุดกลางจอออกไปทุกทิศ (SVG `stroke-dashoffset`) | `noti.mp3` |
| 400–1400 | ข้อความระบบพิมพ์ทีละบรรทัดชิดซ้ายบน | ติ๊กตามตัวอักษร (เบามาก) |
| 1400 | **กลิตช์ 180ms** | `cut_glit.mp3` |
| 1500–2600 | โลโก้ `SE.RA.PH` ประกอบร่างจากเศษข้อมูลที่บินมารวมกัน แล้วนิ่ง + `.glitch-p` | – |
| 2600–4200 | การ์ด **เงาดำ + `???`** ของผู้เล่นทุกคนลอยขึ้นเรียงเป็นวงกลมรอบกลางจอ หมุนช้า ๆ 1 รอบ | `noti2.mp3` |
| 4200 | ม่าน `preTrigger` กวาดเข้า S1 | – |

```
> CONNECTING TO SE.RA.PH ...
> MOON CELL AUTOMATON : ONLINE
> PARTICIPANTS REGISTERED : 6
> IDENTITY MASK : ENABLED
> ELIMINATION CYCLE : 5 DAYS
```

> การ์ดวงกลมเงาดำคือ **ประโยคเปิดของโหมดนี้ทั้งโหมด**: มีคน 6 คน ไม่มีใครรู้ว่าใครเป็นใคร

---

### S1 — แบนเนอร์เปิดวัน · 2.4s (วันแรกของรอบ) / 1.4s (วันถัด ๆ ไป)

ปฏิทิน Persona จับคู่กับการนับถอยหลังของ Moon Cell

| เวลา | ภาพ |
|---|---|
| 0–200 | แถบดำทแยงพาดจากมุมล่างซ้ายขึ้นขวาบน (`skewX(-12deg)`) กวาดเข้าคลุม 70% ของจอ |
| 150–650 | เลข `DAY 3` ตัวยักษ์ (`--font-p-display` italic 900, `clamp(5rem,18vw,12rem)`) พุ่งเข้าจากขวา เกินเป้า 6% แล้วเด้งกลับ |
| 400 | เส้นใต้ทแยงลากจากซ้ายไปขวา 300ms (ใช้ `bannerUnderline` เดิม) |
| 500–900 | บรรทัดรอง fade+slide ขึ้น: `วันสืบสวน · เหลืออีก 2 วันก่อนการคัดออก` |
| 700 | **แถบวัน 5 จุด** (day rail) เลื่อนเข้ามุมขวาบนแล้วค้างเป็น HUD ถาวรทั้งวัน |
| 1800–2400 | แถบดำถอยออกทางขวา เผยกระดาน |

**แถบวัน 5 จุด (day rail)** — HUD ถาวรมุมขวาบนตลอดโหมด
- จุดที่ 1–4 = วงกลมไซแอน · ผ่านแล้วทึบ · วันปัจจุบันมีวงแหวนหมุน (`.p-target-ring` ย่อส่วน)
- **จุดที่ 5 = สัญลักษณ์ดาบไขว้สีแดง เต้นเบา ๆ ตลอดเวลาตั้งแต่วันที่ 1** — เป็นนาฬิกาความตายที่มองเห็นได้

**วันที่ 4 พิเศษ:** บรรทัดรองเปลี่ยนเป็นสีแดง `พรุ่งนี้คือวันคัดออก` + จอสั่น 200ms + `noti2.mp3` แทน `noti.mp3`

---

### S2 — เฟสจั่วไพ่ วันที่ 1–4

ไม่ใช่คัตซีน แต่คือ "ชุดแต่งกาย" ของกระดานที่ต้องต่างจากเกมปกติ

- **พื้นหลัง:** `.sc-grid-faint` เลื่อนแนวทแยงช้า ๆ (60s/รอบ) ทับพื้นหลังกลางวัน/กลางคืนเดิม + วีเนตหนักขึ้น
- **การ์ดผู้เล่นอื่น:** ภาพตัวละคร → `filter: brightness(0)` เป็นเงาดำล้วน · ป้ายชื่อผู้เล่นยังอ่านได้ (ชื่อไม่ใช่ความลับ) · ตรงที่เคยเป็นชื่อตัวละครเป็น `???` · กรอบเป็นเส้นประไซแอนแทนสีประจำตัว
  - คนที่ **เคยเห็นตัวละครนั้นลงสนามแล้ว** → เห็นภาพจริง กรอบสีประจำตัวปกติ (per-viewer จาก `buildStateFor`)
- **HUD ที่หายไป:** หลอดเลือด · เกราะ · แถบแต้มสกิล · ปุ่มสกิล · ปุ่มไอเทม/ปืน — **หายทั้งหมด ไม่ใช่แค่ disable**
  > ปุ่มที่กดไม่ได้แต่ยังอยู่ = ผู้เล่นจะพยายามกดทุกวัน การเอาออกไปเลยคือคำอธิบายกติกาที่ดีที่สุด
- **HUD ที่มาแทน:** แถวเดียวใต้การ์ดตัวเอง — `◆ Matrix 2/4` · `🪙 14` · `📘 ระดับ 2` · `⚡ ความจุ 4/8`
  - ตัวเลขที่เพิ่งเปลี่ยนจะ pop + เรืองแสง 600ms (ใช้ `scorePop` เดิม)
- **อนิเมชันจั่ว:** `dealCard` เดิม + เส้นสแกนไซแอนวิ่งผ่านการ์ดตอนแตะพื้น 250ms
- **เพลง:** `mooncell/theme/day1-4.mp3` (วนตลอดวันที่ 1–4 ทั้ง 4 วัน ไม่เริ่มใหม่ระหว่างวัน)

---

### S3 — เปิดไพ่ · ผู้ชนะประจำวัน · รางวัล · 4.0s

ไม่มีเฟสโจมตี ฉะนั้นฉากนี้ต้องรับน้ำหนักความสะใจทั้งหมดของวันไว้เอง

| เวลา | ภาพ | เสียง |
|---|---|---|
| 0 | เวลาหยุด กระดานหรี่ลง 30% | `trun_change` (เดิม) |
| 0–600 | ไพ่ทุกคนพลิกหงายพร้อมกัน (`rotateY` 3D 400ms, stagger 60ms ตามตำแหน่งที่นั่ง) | เสียงไพ่ไล่กัน |
| 600–1100 | แต้มลอยขึ้นเหนือการ์ดแต่ละใบ เป็นเลขนับวิ่งจาก 0 ถึงค่าจริง 500ms | ติ๊กตามเลข |
| 1100 | **คนไพ่แตก:** เลขเป็นสีแดง + กลิตช์เฉพาะการ์ดใบนั้น + การ์ดสั่น | `cut_glit.mp3` |
| 1200–1800 | **ผู้ชนะ:** วงแหวนทอง 3 ชั้นขยายออกจากการ์ด + การ์ดยกสูงขึ้น 12px + ป้ายทแยง `ผู้ชนะประจำวัน` สไลด์เข้า | เสียงชนะ |
| 1800–3000 | **ฉากรางวัล** (ดูด้านล่าง) | `noti2.mp3` |
| 3000–4000 | ทุกอย่างจางลง → ม่านกวาดเข้า S4 | – |

**ฉากรางวัล — "การจ่ายส่วนแบ่งของ Moon Cell"**
- ลูกบาศก์ข้อมูลไซแอน 1 ก้อนก่อตัวกลางจอ (ประกอบจากเศษข้อมูล 300ms) แล้ว **บินโค้งเข้าไปเสียบในช่อง Matrix ช่องถัดไปของผู้ชนะ** — ช่องนั้นติดไฟพร้อมเสียง
  - ใช้กลไกเดียวกับ `useCardFlights` / `FlyingCardsLayer` ที่มีอยู่แล้ว (คำนวณตำแหน่งจาก DOM rect)
- เหรียญทอง 1 เหรียญบินตามหลัง 200ms เข้าไปที่ตัวเลขเหรียญ → ตัวเลขเด้ง
- **Matrix เต็ม 4/4:** ช่องทั้งแถวสว่างพร้อมกัน + ข้อความระบบ `> MATRIX BUFFER FULL` + แถบเตือนว่าล้นแล้วจะเสียของฟรี

> **ห้ามเปิดเผยตัวละครของผู้ชนะโดยเด็ดขาด** — ป้ายยังเป็น `???` เหมือนเดิม ฉากนี้ฉลอง "ชื่อผู้เล่น" ไม่ใช่ "ตัวตน"

---

### S4 — เลือกสถานที่ · นับถอยหลัง 20s

ฉากเมนูปลายทางแบบ Persona เต็มรูปแบบ — ใช้ภาพ 5 ภาพที่มีอยู่แล้วใน `mooncell/`

**การเข้าฉาก**
- เพลง crossfade `day1-4.mp3` → `mooncell/theme/rest_time.mp3` ใน 600ms
- ม่าน `preTrigger` กวาด → เปิดมาเป็นหน้าเต็มจอ พื้น `.sc-grid`

**ผัง** — การ์ด 5 ใบเรียง **ทแยงเหลื่อมกัน** (ไม่ใช่กริดตรง) แต่ละใบ `skewX(-8deg)` สูงไม่เท่ากันเล็กน้อย
- ลอยเข้าจากขวาทีละใบ stagger 90ms (`.p-rise` + translateX)
- แต่ละใบ: ภาพสถานที่เต็มการ์ด (มืดลง 45%) + ชื่อไทยตัวหนา + ไอคอน + **แถบสถานะบริบท**

| การ์ด | แถบสถานะที่ต้องโชว์ | สภาพ "กดไม่ได้" |
|---|---|---|
| ห้องพัก | `สุ่มของฟรี 1 ชิ้น` | – (กดได้เสมอ) |
| โบสถ์ | `❤️3 🛡️2 ⚡4/8` ค่าปัจจุบันทั้งสาม | – |
| สวนสาธารณะ | `◆ Matrix ที่ถืออยู่ 2` | Matrix = 0 → หรี่ + `ไม่มีแต้มให้ลง` |
| ห้องสมุด | `ระดับทักษะ 2 → 3 · ปลดล็อกสกิลรอง` | ระดับ 6 แล้ว → หรี่ + `สูงสุดแล้ว` |
| ร้านสะดวกซื้อ | `🪙 14` | เหรียญ < ราคาถูกสุด → หรี่ |

**ปฏิสัมพันธ์**
- hover/focus: การ์ดขยาย 1.06 · ภาพในกรอบซูม 1.12 (คนละความเร็ว = มีมิติ) · `.p-scan-hover` วิ่ง · ขอบไซแอนติด
- **แผงคำอธิบายด้านขวา** สไลด์เปลี่ยนเนื้อหาตามการ์ดที่ชี้อยู่ (150ms) — บอกผลเต็ม ๆ ของสถานที่นั้น
- กดเลือก: การ์ดพุ่งเข้ากลางจอขยายเต็มจอ 400ms + แฟลชขาว + `cut_glit.mp3` → เข้า S5.x

**ระหว่างรอคนอื่น**
- แถบล่างจอ: จุดวงกลม 1 จุดต่อผู้เล่น — เลือกแล้ว = ทึบ + เครื่องหมายถูก · ยังไม่เลือก = กระพริบ
- **ไม่บอกว่าใครเลือกที่ไหน** — สถานที่ที่คู่แข่งไป คือข้อมูลสืบสวนที่มีค่าที่สุดในโหมดนี้ ต้องปิดไว้
- หมดเวลา = สุ่มให้ + ข้อความระบบ `> AUTO-ASSIGNED` (ตามธรรมเนียมเดิมของเฟส `ATTACK`)

---

### S5 — ฉากในสถานที่ (5 ฉากย่อย)

**โครงร่วมทุกฉาก:** ภาพสถานที่เป็นฉากหลังเต็มจอ (blur 3px + Ken Burns ซูมช้า 1.0→1.06 ตลอดฉาก) · แผงโต้ตอบลอยหน้า · จบด้วยการ์ดสรุปผล 800ms แล้วม่านกวาดกลับสนาม

#### S5.1 ห้องพัก — ของฟรี 1 ชิ้น · 2.8s

| เวลา | ภาพ |
|---|---|
| 0–500 | ลังข้อมูลไซแอนลอยเข้ากลางจอ หมุนช้า มีเส้นตารางวิ่งบนผิว |
| กดเปิด | ลังแตกเป็นเศษ polygon กระจายออก + ลำแสงพุ่งขึ้นจากจุดกลาง 400ms |
| +400 | ไอเทมลอยขึ้นช้า ๆ พร้อมชื่อ + คำอธิบาย + รัศมีสีตามระดับของ |
| +1200 | การ์ดสรุป `ได้รับ: <ชื่อของ>` |

- **ของระดับสูง (ราคาเกิน 3 มาก):** เพิ่มวงแหวนทอง 3 ชั้น + จอสั่น + `cut_glit.mp3` — ต้องรู้สึกว่า "ได้ของดี" ทันทีโดยไม่ต้องอ่านราคา
- เสียงมาตรฐาน: `noti2.mp3`

#### S5.2 โบสถ์ — เพิ่มความจุ · 3.4s

- ลำแสงจากกระจกสีส่องลงกลางจอ (gradient เคลื่อน + ฝุ่นแสงลอย — ต่อยอด `.p-particle`)
- **แท่นเลือก 3 แท่น** เรียงหน้ากล้อง แต่ละแท่นโชว์ `ค่าปัจจุบัน → ค่าใหม่` ตัวใหญ่

| แท่น | แสดง | หมายเหตุพิเศษ |
|---|---|---|
| ❤️ พลังชีวิต | `3 → 4` | |
| 🛡️ เกราะ | `2 → 3` | |
| ⚡ ความจุแต้มสกิล | `4 → 5` (เพดาน 8) | **ถ้ายังต่ำกว่า 6 ต้องขึ้นแถบเตือนสีเหลืองใต้แท่น:** `ท่าไม้ตายต้องการ 6 — ตอนนี้เจ้าร่ายไม่ได้` |

> แถบเตือนนี้คือคำสั่งตรงจากสเปกกติกา §3 ("นี่คือความตั้งใจของดีไซน์ ไม่ใช่บั๊ก แต่ UI ต้องสื่อสารให้ชัด")
> **ต้องขึ้นทุกครั้งที่ความจุ < 6 ไม่ว่าผู้เล่นจะเลือกแท่นไหน**

- เลือกแล้ว: แท่นสว่าง → ลำแสงพุ่งเข้าหาตัวผู้เล่น → **หลอดค่านั้นงอกช่องใหม่ออกมาด้วยอนิเมชัน pop + เรือง** (เห็นช่องเพิ่มจริง ๆ ไม่ใช่แค่เลขเปลี่ยน)
- เสียง: `noti.mp3` ตอนชี้ · `noti2.mp3` ตอนยืนยัน

#### S5.3 สวนสาธารณะ — ลง Matrix · 4.5s ★ ฉากเด่นของครึ่งแรก

ฉากนี้คือ "การสอดแนม" ต้องให้ความรู้สึกเหมือนนั่งอยู่หน้าจอเรดาร์ตอนตีสาม

| เวลา | ภาพ |
|---|---|
| 0–600 | ภาพสวนจางลงเป็นพื้นดำ + วงเรดาร์ไซแอน 3 วงขยายออกจากกลางจอเป็นจังหวะ |
| 600 | **เงาดำของผู้เล่นทุกคนเรียงเป็นวงกลมรอบตัวเรา** แต่ละคนมีกรอบข้อมูลเล็ก ๆ (ชื่อผู้เล่น + `???`) |
| — | คลัง Matrix ของเราอยู่ล่างจอ เป็นลูกบาศก์ไซแอนเรียงกัน หยิบได้ |

**การลงแต้ม** — ลากลูกบาศก์ไปวางบนเป้า หรือกดเป้าแล้วกดจำนวน (รองรับทั้งเมาส์และสัมผัส)

ทุกครั้งที่ลง 1 แต้ม: เส้นข้อมูลวิ่งจากเราไปหาเป้า 300ms → วงแหวนล็อกเป้าซ้อนเพิ่มอีก 1 ชั้นรอบเงานั้น (`.p-target-ring` ซ้อนกัน) → `noti2.mp3`

| แต้มสะสมบนเป้า | ภาพที่เกาะติดเป้าหมายตัวนั้นถาวร | เสียง |
|---|---|---|
| 1 | ไอคอน 🂠 ที่มุมกรอบ + ข้อความ `เห็นจำนวนไพ่` | `noti2` |
| 2 | ครึ่งวงโล่ไซแอนคลุมเงา + `รับดาเมจน้อยลง 1` | `noti2` |
| 3 | **วงแหวนแดง + ไอคอนตาแดง** + ข้อความเตือนตัวใหญ่ | `cut_glit` |

**การยืนยันระดับ 3 (สำคัญ)** — ก่อนลงแต้มที่ 3 ต้องขึ้นแผงยืนยันเต็มจอ:

```
> DEEP SCAN — LEVEL 3
เจ้าจะเห็นแต้มของเป้าหมายตลอดการดวล
แต่เป้าหมายจะรู้ว่ามีคนกำลังจับตาเขาอยู่
[ ยืนยัน ]   [ ยกเลิก ]
```

> การแลกเปลี่ยนนี้คือหัวใจของระบบ Matrix ทั้งระบบ ถ้าผู้เล่นกดพลาดโดยไม่รู้ตัว กลไกทั้งอันจะพัง

- จบฉาก: กล้องถอยออก แผนผังการจับตาทั้งหมดของเราสรุปเป็นการ์ดใบเดียว (`เป้าหมาย 2 คน · แต้มที่ลง 3`)

#### S5.4 ห้องสมุด — ระดับทักษะ +1 · 3.2s (หรือ 5.0s ถ้าปลดล็อก)

- ชั้นหนังสือในภาพ → แผ่นข้อมูลลอยออกมาเปิดกางกลางจอ → เส้นตัวอักษรไซแอนวิ่งจากแผ่นเข้าตัวผู้เล่น 800ms
- **แถบระดับทักษะเป็นบันได 6 ขั้น** ขั้นที่ผ่านแล้วทึบ · ขั้นใหม่ติดไฟไล่จากซ้ายไปขวา + เสียงไต่ระดับ
- ขั้นที่ 3 กับ 6 มีสัญลักษณ์กุญแจ — เห็นตั้งแต่ระดับ 1 ว่ายังต้องไปอีกไกลแค่ไหน

**ถ้าขึ้นถึงระดับ 3 หรือ 6 → ฉากปลดล็อกใหญ่ (+1.8s)**

| เวลา | ภาพ | เสียง |
|---|---|---|
| 0 | จอกลิตช์ 180ms | `cut_glit.mp3` |
| 180 | ข้อความระบบ `> SKILL SLOT UNLOCKED` | – |
| 400–1200 | **การ์ดสกิลพลิกเข้ามาจากด้านข้าง** โชว์ชื่อสกิล + ไอคอน + คำอธิบาย + ราคา (4 หรือ 6) | `noti2.mp3` |
| 1200–1800 | การ์ดหมุนไปเข้าที่ในแถบสกิลของผู้เล่น | – |

> นี่คือ **ครั้งแรกที่ผู้เล่นได้เห็นสกิลของตัวเอง** ตลอดทั้งโหมด (เพราะวันที่ 1–4 ไม่มีแถบสกิล) — ฉากนี้จึงคุ้มค่าที่จะยาว

**ระดับ 6 + ความจุ < 6:** ต่อท้ายด้วยแถบเตือนแดง `⚡ ความจุของเจ้าคือ 4 — ท่านี้ต้องการ 6 · ไปเพิ่มที่โบสถ์`

#### S5.5 ร้านสะดวกซื้อ — ซื้อของ

- ภาพร้านเป็นฉากหลัง แผงร้านค้ามายาเดิมแต่งใหม่เป็นชั้นวางของ/ตู้แช่
- การ์ดของเรียงเป็นชั้นแนวนอน เลื่อนได้ (`.p-scroll`)
- ซื้อ: การ์ดพุ่งลงไอคอนกระเป๋า + เหรียญนับถอยลงแบบ count-down + `noti2.mp3`
- ซื้อไม่ไหว: การ์ดหรี่ + ป้ายราคาแดง + สั่นซ้าย-ขวา 200ms ตอนพยายามกด

---

### S6 — ประกาศคู่ดวล (จบวันที่ 2) · 7.0s ★★ ฉากใหญ่ที่สุดของครึ่งแรก

หยุดเกมทุกคน (ระดับ T3 · เข้า `runCutsceneQueue`)

| เวลา | ภาพ | เสียง |
|---|---|---|
| 0 | **เพลงหยุดสนิท** จอค้างนิ่ง | ความเงียบ 400ms |
| 400–900 | จอมืดลงจากขอบเข้ากลาง เส้นตารางแดงเรืองขึ้นมาแทน | เสียงหึ่งต่ำไต่ขึ้น |
| 900–2000 | ข้อความระบบพิมพ์กลางจอ | ติ๊กตามตัวอักษร |
| 2000 | กลิตช์ + กล้องซูมออกอย่างแรง | `cut_glit.mp3` |
| 2000–5600 | **ตัดเข้าทีละคู่** 900ms/คู่ (ดูด้านล่าง) | `noti2.mp3` ต่อคู่ |
| 5600–6400 | กล้องถอยสุด เห็นแผนผังคู่ดวลทั้งหมดพร้อมกัน | – |
| 6400–7000 | ตัวนับ `อีก 3 วัน` ตัวใหญ่ขึ้นทับ แล้วจอตัดกลับสนาม | `noti2.mp3` ยาว |

```
> ELIMINATION PROTOCOL INITIALIZED
> PAIRING COMPLETE
```

**การตัดเข้าทีละคู่ (900ms)**
- การ์ดเงาดำ 2 ใบพุ่งเข้าจากซ้ายและขวา **ชนกันกลางจอ** → แฟลชขาว 80ms → จอสั่น
- ป้าย `VS` ทแยงแดงตัวใหญ่ผ่ากลาง
- ชื่อ **ผู้เล่น** ขึ้นใต้การ์ดแต่ละใบ (ตัวละครยังเป็น `???` — เปิดเผยวันที่ 5 เท่านั้น)
- คู่ที่มีตัวเราอยู่ด้วย: กรอบแดงหนาขึ้น + จอสั่นแรงกว่า + `cut_glit.mp3` แทน `noti2`

**คนที่ผ่านฟรี (จำนวนคนเป็นเลขคี่)**
- การ์ดลอยขึ้นเหนือแผนผัง มีลำแสงทองส่องลง + ป้าย `ผ่านโดยไม่ต้องดวล`
- ข้อความระบบ `> BYE — NO OPPONENT ASSIGNED`

**ผลถาวรหลังฉากนี้ (วันที่ 3–4)**
- HUD มุมซ้ายบนขึ้นป้ายถาวร: `⚔ คู่ของเจ้า: <ชื่อผู้เล่น>` ขอบแดง เต้นช้า ๆ
- **การ์ดของคู่เราในสนามมีกรอบแดงเรืองตลอด 2 วันที่เหลือ** — ทุกครั้งที่มองกระดานจะเห็นเขาก่อนใคร
- ที่สวนสาธารณะ การ์ดคู่ดวลของเราถูกดันขึ้นมาเป็นเป้าแรกของวงเรดาร์

---

### S7 — เข้าวันที่ 5 · 3.6s ★★ จุดหักของทั้งรอบ

ต้องรู้สึกเหมือนเกมคนละเกมกับ 4 วันที่ผ่านมา

| เวลา | ภาพ | เสียง |
|---|---|---|
| 0 | **เพลง `day1-4.mp3` ตัดจบกลางคัน** (hard cut ไม่ fade) | ความเงียบ 250ms |
| 250 | จอทั้งจอแตกเป็นเสี่ยงกระจก เศษกระเด็นออก เผยพื้นหลังใหม่: สนามดวล SE.RA.PH ตารางแดง `.sc-grid-hot` | `cut_glit.mp3` |
| 400 | เพลงวันดวลเริ่ม (ดู §6) | `day_mooncell` / `night_mooncell` |
| 400–1200 | ตัวอักษรยักษ์ `DAY 5` พุ่งขึ้นจากล่าง แล้วมีคำว่า `ELIMINATION` ทับทแยงสีแดง | – |
| 1200–2600 | **หลอดเลือด + เกราะของผู้เล่นทุกคนโผล่กลับมา** ไล่ทีละคนตามตำแหน่งที่นั่ง stagger 140ms — หลอดวาดตัวเองจากซ้ายไปขวาพร้อมเสียง | เสียงไล่ทีละคน |
| 2600–3200 | แถบสกิล + ไอเทม + ปุ่มทริกเกอร์สี สไลด์ขึ้นจากขอบจอล่าง | – |
| 3200–3600 | ข้อความระบบ `> COMBAT SYSTEMS RESTORED` แล้วจางไป | `noti2.mp3` |

> ท่อน 1200–2600 คือ **ผลตอบแทนของการซ่อนหลอดเลือดมา 4 วัน** — 4 วันที่ผ่านมาไม่มีอะไรทำร้ายเราได้ ตอนนี้มีแล้ว
> ถ้าไม่ซ่อนมาก่อน ฉากนี้จะไม่มีน้ำหนักอะไรเลย

---

### S8 — เรียกคู่ลงสนาม + เปิดเผยตัวละคร · 6.4s/คู่ ★★★ ฉากแพงที่สุดของโหมด

**8a. หน้าคิวดวล (ระหว่างรอ)**
- ผู้ชมเห็นแถบคิวด้านซ้าย: รายชื่อคู่ทั้งหมด · คู่ที่ดวลจบแล้วมีกากบาททับผู้แพ้ · คู่ถัดไปเรืองแสง
- กลางจอ: สนามดวลว่างเปล่า ตารางแดงหมุนช้า
- ป้าย `คู่ต่อไป` + นับถอยหลัง 3 วินาที

**8b. เรียกคู่ (1.4s)**
- ชื่อผู้เล่น 2 คนพุ่งเข้าจากซ้าย/ขวา ชนกลางจอ + `VS` ทแยงแดง + จอสั่น + `cut_glit.mp3`
- กล้องแพนลงสู่พื้นสนาม

**8c. ฉากเปิดเผยตัวละคร (2.2s × 2 คน) — Servant Reveal**

ทำทีละคน ซ้ายก่อน ขวาตาม

| เวลา | ภาพ | เสียง |
|---|---|---|
| 0–300 | เงาดำของผู้เล่นยืนกลางจอ (ขนาดเต็มตัว) ตารางไซแอนล้อมรอบ | หึ่งต่ำ |
| 300–1000 | **เส้นข้อมูลไซแอนวิ่งไล่ขึ้นตามรูปร่าง จากเท้าถึงหัว** (mask reveal ล่าง→บน) เผยสีจริงตามที่เส้นผ่าน | เสียงสแกนไต่ขึ้น |
| 1000–1150 | เส้นถึงยอด → **กลิตช์แรง** + แฟลชขาวเต็มจอ | `cut_glit.mp3` |
| 1150–1600 | เงาที่เหลือแตกเป็นเศษข้อมูลกระจายออก → **ภาพตัวละครจริง** ปรากฏเต็มตัว ซูมจาก 1.15 → 1.0 | เสียงกระแทก |
| 1600–2000 | ป้ายชื่อตัวละครสไลด์เข้าจากล่าง + เส้นใต้ทแยง (`.p-name-tag` ขยายส่วน) + รัศมีสีประจำตัวแผ่ออก | `noti2.mp3` |
| 2000–2200 | ตัวละครเลื่อนไปยืนประจำที่ฝั่งของตน | – |

**กฎการเห็น (ต่อผู้ชมแต่ละคน — ใช้ `onlyFor` ของ `queueCutscene`)**

| ผู้ชม | เห็นอะไร |
|---|---|
| ยังไม่เคยเห็นตัวละครนี้ | ฉากเปิดเผยเต็ม 2.2s ตามตาราง |
| เคยเห็นแล้ว | ข้ามการสแกน — ขึ้นภาพจริงพร้อมป้ายชื่อทันที **แต่ยังค้างจอครบ 2.2s** |
| ตัวผู้ดวลเอง | เห็นตัวเองเต็ม (เป็นช็อตเท่ของตัวเอง ไม่ใช่การเปิดเผย) |

> **ห้ามลดเวลาให้คนที่เคยเห็นแล้ว** — ทุกคนต้องออกจากฉากพร้อมกัน ไม่งั้นหลุดซิงก์ (กฎเดียวกับ `lowQ` ในเอนจินเดิม)

**8d. ประจันหน้า (1.0s)**
- กล้องถอย เห็นตัวละครทั้งสองยืนหันหน้าเข้าหากันคนละฝั่งจอ
- `VS` ทแยงผ่ากลาง + ชื่อตัวละครทั้งสองข้าง + เส้นแรงพุ่งเข้ากลาง (`.p-intro-burst` สีแดง)
- ตัดเข้าการดวลจริง

---

### S9 — ระหว่างการดวล

**มุมมองผู้ดวล** — กระดานเต็มรูปแบบ HUD ครบ (สกิล · ไอเทม · ทริกเกอร์สี · หลอดเลือด/เกราะ) เหมือนเกมปกติ + เลเยอร์ SE.RA.PH

**มุมมองผู้ชม** — กระดานเดียวกัน ไม่มีปุ่มใด ๆ
- แถบบนจอ: `👁 กำลังชม — <ชื่อ A> ปะทะ <ชื่อ B>`
- เห็นทั้งคู่เต็ม ๆ ทั้งไพ่และแต้ม (ผู้ชมมีสิทธิ์รู้ทุกอย่างของคู่ที่ลงสนาม — ตามสเปกกติกา §7)
- **ผู้เล่นที่ยังไม่ได้ดวล จะจดจำสิ่งที่เห็นไว้ใช้รอบหน้าได้** — นี่คือเหตุผลที่ระบบเปิดเผยเป็นแบบสะสม

**เลเยอร์ SE.RA.PH ระหว่างดวล**
- ทุกดาเมจที่เข้า → ตารางพื้นหลังกระเพื่อมเป็นคลื่นวงกลมออกจากจุดที่โดน 400ms
- ทุกท่าไม้ตาย → ตารางทั้งจอเปลี่ยนเป็นสีของตัวละครนั้น 1 วินาทีแล้วค่อยกลับ
- เลือดเหลือ 1 → ขอบจอของคนนั้นเป็นกรอบแดงเต้นตามจังหวะหัวใจ + ตารางแตกเป็นรอยร้าว

**HUD ของ Matrix (ผลจากที่ลงไว้วันที่ 1–4)**

| ระดับ | ฝั่งคนที่ลง Matrix | ฝั่งเป้าหมาย |
|---|---|---|
| 1 · เห็นจำนวนไพ่ | ตัวเลขจำนวนไพ่ลอยข้างการ์ดเป้า ในกรอบเส้นประไซแอน + ไอคอน 🂠 | ไม่รู้ตัว |
| 2 · ลดดาเมจ 1 | – | ตอนโดนตี ตัวเลขดาเมจโชว์ `2` ขีดฆ่าแล้วเป็น `1` + โล่ไซแอนวาบ |
| 3 · เห็นแต้ม | แต้มเป้าโชว์ตลอดในกรอบไซแอน + เส้นสแกนวิ่งผ่านตัวเลขทุก 2 วิ | **กรอบแดงบาง ๆ รอบจอตลอดการดวล + ไอคอนตาแดงกระพริบมุมขวาบน** |

**ฉากแจ้งเตือน "ถูกจับตา" (เฉพาะเป้าหมายระดับ 3 · ตอนเริ่มดวล · 1.6s)**

```
> UNAUTHORIZED OBSERVER DETECTED
มีคนกำลังจับตาแต้มของเจ้าอยู่
```

- ขึ้นทับกลางจอ ตัวหนังสือแดง สั่นเบา ๆ + `noti.mp3`
- **ไม่บอกว่าใคร** — ถ้าบอก ระบบสืบสวนทั้งระบบจะหมดความหมาย

---

### S10 — ตกรอบ · 4.0s ★★ ผู้แพ้ถูก "ลบ" ไม่ใช่ "ตาย"

| เวลา | ภาพ | เสียง |
|---|---|---|
| 0–400 | หมัดสุดท้ายเข้า → **สโลว์โมชัน** (ทุกอย่างช้าลงเหลือ 25%) | เสียงยืดต่ำลง |
| 400–700 | จอกลายเป็นขาวดำทั้งจอ | เงียบ |
| 700–2100 | **ตัวละครผู้แพ้แตกเป็นบล็อกพิกเซล ไล่จากล่างขึ้นบน หายไปทีละแถว** (14 แถว × 100ms) | `cut_glit.mp3` ยืดยาว |
| 1200–2600 | ข้อความระบบพิมพ์ทีละตัวอักษรสีแดงกลางจอ | ติ๊ก |
| 2600–3000 | การ์ดของเขาในผังคู่ดวลถูกขีดกากบาททแยง 2 เส้น | – |
| 3000–4000 | **ผู้ชนะ:** สียังคงอยู่คนเดียวในจอขาวดำ · ตารางไซแอนประกอบใหม่รอบตัว · ป้าย `ผ่านเข้ารอบถัดไป` | `noti2.mp3` |

```
> PLAYER <ชื่อ> — DELETED FROM SE.RA.PH
```

- ผู้เล่นที่ตกรอบ: จอค้างที่ฉากนี้เพิ่มอีก 2 วินาที แล้วเปลี่ยนเป็นโหมดผู้ชมถาวร (`เจ้าถูกลบแล้ว — เฝ้าดูผู้รอดคนอื่นต่อไป`)

---

### S11 — จบรอบ · 3.2s

| เวลา | ภาพ |
|---|---|
| 0–800 | ผังสรุป: การ์ดผู้รอดชีวิตทั้งหมดเรียงกัน · คนที่ออกไปแล้วเป็นเงาจาง มีกากบาท |
| 800–1600 | **เหรียญ +5 บินเข้าทุกคนพร้อมกัน** จากขอบจอ → ตัวเลขเหรียญของทุกคนนับขึ้น |
| 1200–2200 | หลอดเลือด + เกราะไหลเติมเต็มทุกคนพร้อมกัน (สีเขียวไล่จากซ้าย) |
| 1600–2400 | **Matrix ที่ลงบนเป้าหมายสลายเป็นเศษข้อมูลลอยหายไป** · แต้มที่ยังไม่ได้ลงยังอยู่ในคลัง เรืองแสงค้างไว้ |
| 2400–3200 | ข้อความระบบ + จอมืด → S1 ของรอบใหม่ |

```
> CYCLE COMPLETE — SURVIVORS: 4
> NEXT CYCLE INITIALIZING ...
```

> ท่อน 1600–2400 ต้องแยกให้ชัดด้วยภาพ ว่า **อะไรหายและอะไรอยู่ต่อ** — เป็นจุดที่ผู้เล่นสับสนง่ายที่สุดในสเปกทั้งฉบับ

**สลับกลางวัน/กลางคืน:** รอบใหม่ = อีกช่วงเวลาหนึ่ง → ทั้งรอบเปลี่ยนโทนสี (ดู §6)

---

### S12 — ผู้ชนะคนสุดท้าย · 8.0s

| เวลา | ภาพ |
|---|---|
| 0–1500 | ตารางทั้งจอค่อย ๆ สลายเป็นอนุภาคลอยขึ้น |
| 1500–3000 | ลำแสงขาวจากด้านบนส่องลงบนตัวละครผู้ชนะ · ตัวละครลอยขึ้นช้า ๆ |
| 3000–4500 | ข้อความระบบ + ชื่อผู้เล่น + ชื่อตัวละคร ตัวใหญ่ |
| 4500–8000 | การ์ดสถิติทั้งแมตช์ไล่ขึ้นทีละใบ |

```
> MOON CELL AUTOMATON — VERDICT
> SOLE SURVIVOR CONFIRMED
> WISH GRANTED.
```

**การ์ดสถิติ:** รอบที่ผ่าน · ดวลชนะกี่คู่ · Matrix ที่ลงทั้งหมด · ระดับทักษะสุดท้าย · เหรียญคงเหลือ · สถานที่ที่ไปบ่อยที่สุด

---

## 5. ระบบแจ้งเตือน 4 ระดับ

| ระดับ | ชื่อ | ตำแหน่ง | เวลา | หยุดเกมไหม | ใช้กับ |
|---|---|---|---|---|---|
| **T0** | Toast | มุมขวาบน ซ้อนกันได้ 3 ใบ | 2.0s | ไม่ | เหรียญ +1 · Matrix +1 · ของเข้ากระเป๋า · เตือนเวลาใกล้หมด |
| **T1** | Board card | กลางกระดานล่าง | 2.5s | ไม่ | ผลสถานที่ของเรา · ใครใช้สกิลระหว่างดวล · ทริกเกอร์สีติด |
| **T2** | Banner | เต็มจอโปร่ง | 2.0–3.0s | หยุดอินพุต ไม่หยุด timer | เปิดวัน (S1) · ผู้ชนะประจำวัน (S3) · ปลดล็อกสกิล (S5.4) · ถูกจับตา (S9) |
| **T3** | Cutscene | เต็มจอทึบ | 4.0–8.0s | **หยุดเกมจริง** เข้า `runCutsceneQueue` | S0 · S6 · S7 · S8 · S10 · S11 · S12 |

**กฎร่วม**
- T0/T1 ห้ามซ้อนกันเกิน 3 ใบ — เกินให้เข้าคิว
- T2 ห้ามซ้อนกันเลย — ต้องต่อคิว
- T3 ใช้คิวของเอนจินเดิม (`cutsceneQueue`) และ **ต้องเข้าคิวหลังบรรทัดที่ `dealRound()` ล้างคิวเสมอ** (Gotcha #1 ของ GAME_SYSTEM)
- ทุกระดับต้องมีเสียงคู่กัน ไม่มีการแจ้งเตือนเงียบ
- ทุกระดับต้องอ่านออกในจอมือถือแนวตั้ง (โหมดนี้มีตัวเลขให้จำเยอะกว่าเกมปกติมาก)

---

## 6. เพลงและเสียง

**ไฟล์ที่มีอยู่แล้ว** (`client/public/mooncell/`) — เพิ่มเข้า `FILES` ใน [client/src/audio.js](client/src/audio.js)

| คีย์ที่เสนอ | ไฟล์ | ใช้ตอน |
|---|---|---|
| `sc_day` | `theme/day1-4.mp3` | วันที่ 1–4 ตลอดช่วงจั่วไพ่และสรุปผล (S1–S3) |
| `sc_rest` | `theme/rest_time.mp3` | หน้าเลือกสถานที่ + ฉากในสถานที่ (S4–S5) |
| `sc_duel_day` | `day5theme/day_mooncell.mp3` | วันที่ 5 — **รอบที่ตรงกับช่วงกลางวัน** |
| `sc_duel_night` | `day5theme/night_mooncell.mp3` | วันที่ 5 — **รอบที่ตรงกับช่วงกลางคืน** |
| `sc_glitch` | `sond_effect/cut_glit.mp3` | ทุกกลิตช์ (ดูกฎข้อ 2 ใน §2) |
| `sc_noti` | `sond_effect/noti.mp3` | แจ้งเตือนทั่วไป T0/T1/T2 · เปิดวัน · ชี้ตัวเลือก |
| `sc_noti2` | `sond_effect/noti2.mp3` | ยืนยัน/ได้รับของ/สำเร็จ — เสียงที่ "ได้อะไรมา" |

**การเลือกเพลงวันที่ 5 — ตรงกับวงจรวัน/คืนเดิมพอดีเป๊ะ**

> `CYCLE_TURNS = 5` (สลับกลางวัน/กลางคืนทุก 5 เทิร์น) และ 1 รอบของโหมดนี้ = 5 วัน
> **ถ้ากำหนดให้ 1 วัน = 1 เทิร์นของเอนจินเป๊ะ ๆ → 1 รอบ = 1 ช่วงเวลาพอดี**
>
> - รอบที่ 1 = กลางวันทั้งรอบ → วันที่ 5 เล่น `sc_duel_day`
> - รอบที่ 2 = กลางคืนทั้งรอบ → วันที่ 5 เล่น `sc_duel_night`
> - สลับไปเรื่อย ๆ
>
> **ผลพลอยได้:** โทนสีของทั้งรอบเปลี่ยนตามด้วย (รอบกลางวัน = ตารางไซแอน/ฟ้า · รอบกลางคืน = ตารางม่วง/คราม)
> ทำให้ผู้เล่นรู้ว่า "รอบนี้รอบที่เท่าไหร่" โดยไม่ต้องอ่านตัวเลข
>
> ⚠️ **เงื่อนไขที่ต้องรักษาไว้:** 1 วัน ต้อง = 1 เทิร์นเป๊ะ (เฟสเลือกสถานที่ต้องไม่นับเป็นเทิร์นแยก)
> ไม่งั้นการเรียงตัวนี้จะเลื่อนและพังทั้งระบบ

**การจัดการเพลง**
- `sc_day` ↔ `sc_rest` สลับกันวันละหลายครั้ง → ใส่ทั้งคู่ใน `MUSIC_POSITION_GROUPS` **กลุ่มเดียวกัน** เพื่อให้เล่นต่อจากวินาทีเดิม ไม่เริ่มใหม่ทุกวัน
- `sc_duel_day` / `sc_duel_night` อยู่คนละกลุ่ม → **เริ่มจากต้นเสมอ** ตอนเข้า S7 (ต้องการความกระแทก)
- S6 และ S7 ต้อง `stopMusic()` จริง ๆ ก่อนเข้าฉาก — ความเงียบคือส่วนหนึ่งของฉาก
- เพลงตัวละคร (`activeSkillMusic()`) ทับได้ตามปกติ **เฉพาะวันที่ 5** — วันที่ 1–4 ไม่มีสกิลจึงไม่มีทางถูกเรียก (ตรงกับสเปกกติกา §14 ข้อ 2)

---

## 7. โค้ดที่เขียนแล้ว — ไฟล์ · คลาส · วิธีดูฉาก

> **สถานะ: เลเยอร์ภาพเขียนเสร็จแล้ว** (ฝั่ง server ของโหมดยังไม่ได้ทำ — ดู §8/§10)
> ทุกคอมโพเนนต์เป็น presentational ล้วน รับ props อย่างเดียว ไม่แตะ socket → ต่อเข้า server ทีหลังได้โดยไม่ต้องแก้

### วิธีดูฉากทั้งหมดตอนนี้

```
cd client && npm run dev     ->  http://localhost:5173/?seraph
```

`?seraph` เปิดหน้า `seraph/Preview.jsx` ซึ่งใช้ข้อมูลปลอมในตัวเอง ไม่ต่อ socket ไม่แตะเกมจริง
มีปุ่มเล่นทีละฉาก · ปุ่มเล่นซ้ำ · สวิตช์ "รอบกลางคืน" (สลับฉากหลัง+เพลง) · สวิตช์ "โดน Matrix ระดับ 3 จ้องอยู่"

### ไฟล์

| ไฟล์ | เนื้อหา |
|---|---|
| `client/src/seraph/seraph.css` | คลาส `.sc-*` + keyframes ทั้งหมด (โหลดหลัง `index.css` เสมอ ดู `main.jsx`) |
| `client/src/seraph/assets.js` | ทะเบียนไฟล์สื่อ + ตัวโหลด 3 ระลอก + `backgroundFor(phase, night)` |
| `client/src/seraph/ui.jsx` | `SystemLine(s)` · `GlitchCut` · `SeraphBackground` · `DayRail` · `ShadowPortrait` · `MatrixSlots` · `DataCube` · `ToastStack`/`useToasts` · `WatchedFrame` |
| `client/src/seraph/scenes.jsx` | `SeraphBoot`(S0) · `DayBanner`(S1) · `PairingScene`(S6) · `Day5Intro`(S7) · `CharacterReveal`(S8c) · `DeletionScene`(S10) |
| `client/src/seraph/PlaceSelect.jsx` | S4 เลือกสถานที่ (การ์ด 5 ใบ + นับถอยหลัง + สุ่มให้เมื่อหมดเวลา) |
| `client/src/seraph/MatrixRadar.jsx` | S5.3 เรดาร์ Matrix + แผงยืนยันระดับ 3 |
| `client/src/seraph/Preview.jsx` | หน้าดูฉาก (`?seraph`) |

**ที่แก้ไฟล์เดิม (เล็กน้อย):** `main.jsx` (โหลด css + เส้นทาง `?seraph`) · `index.css` (เพิ่ม 4 โทเคนสีใน `@theme`) · `audio.js` (เพิ่มคีย์ `sc_*` 7 ตัว + กลุ่มตำแหน่งเพลง `seraph_field`)

### รายชื่อคลาส/keyframes ที่มีจริงในไฟล์

```
/* พื้นและบรรยากาศ */
.sc-grid / .sc-grid-faint / .sc-grid-hot     @keyframes scGridDrift
.sc-vignette                                  (static)
.sc-shard                                     @keyframes scShardFly

/* กลิตช์ (คู่กับ cut_glit.mp3 เสมอ) */
.sc-glitch                                    @keyframes scGlitchSlice, scGlitchRGB
.sc-shatter                                   @keyframes scShatter        (S7 จอแตก)
.sc-dissolve                                  @keyframes scPixelDissolve  (S10 ลบผู้เล่น)

/* ตัวตนและการเปิดเผย */
.sc-silhouette                                (filter: brightness(0))
.sc-reveal-scan                               @keyframes scRevealScan     (S8 เส้นไล่ขึ้น)
.sc-reveal-burst                              @keyframes scRevealBurst

/* ข้อความระบบ */
.sc-sysline                                   @keyframes scTypewriter (steps())
.sc-sysline-red

/* ฉากวันและคู่ดวล */
.sc-day-num                                   @keyframes scDayNumIn       (S1 เลขวันพุ่งเข้า)
.sc-day-rail / .sc-day-dot / .sc-day-dot-duel @keyframes scDuelDotBeat
.sc-vs                                        @keyframes scVsSlam         (S6, S8)
.sc-pair-clash                                @keyframes scPairClash

/* Matrix */
.sc-matrix-cube                               @keyframes scCubeFloat
.sc-matrix-flight                             (ใช้ FlyingCardsLayer เดิม)
.sc-radar-ring                                @keyframes scRadarPing      (S5.3)
.sc-watched-frame                             @keyframes scWatchedPulse   (S9 กรอบแดง "ถูกจับตา")

/* สถานที่ */
.sc-place-card                                (ต่อยอด .p-scan-hover)
.sc-place-bg                                  @keyframes scKenBurns
.sc-capacity-grow                             @keyframes scCapacityPop    (S5.2 ช่องใหม่งอก)
.sc-skill-unlock                              @keyframes scSkillCardFlip  (S5.4)

/* วันที่ 5 */
.sc-hpbar-restore                             @keyframes scBarDraw        (S7 หลอดวาดตัวเอง)
.sc-damage-ripple                             @keyframes scRipple         (S9 คลื่นบนตาราง)
```

**ข้อตัดสินใจตอนเขียนจริง**
- `.sc-grid*` เลื่อนด้วย `transform` อย่างเดียวตามที่วางไว้ — ไม่ขยับ `background-position` (repaint ทุกเฟรม กิน CPU บนมือถือ)
- **`.sc-dissolve` เปลี่ยนจากแผนเดิม:** สเปกร่างแรกบอกให้ใช้ `clip-path` แถบเดียว แต่ของจริงใช้ **14 ชั้น `background-position`**
  เพราะทางเดิมต้องพึ่ง `mask-composite` ซึ่งซัพพอร์ตยังไม่ทั่ว — 14 element ที่เล่นครั้งเดียวตอนมีคนตกรอบถือว่าถูกมาก
- `prefers-reduced-motion` ปิดเฉพาะอนิเมชันที่ **วิ่งวนตลอด** (ตาราง · เรดาร์ · noise · จอสั่น · ลูกบาศก์)
  ฉากเล่าเรื่องแบบเล่นครั้งเดียวยังเล่นปกติ — ไม่งั้นผู้เล่นกลุ่มนี้จะไม่เห็นเนื้อเรื่องเลย
- `GlitchCut` คุมด้วย mount/unmount + `playId` เพราะ **GIF วนลูปเองไม่มีวันจบ** สั่งให้เล่นรอบเดียวไม่ได้

---

## 8. ข้อมูลที่ server ต้องส่งเพิ่ม

ฉากทั้งหมดข้างบนต้องการฟิลด์ใหม่ใน `buildStateFor(viewerId)` — ระบุไว้ให้ครบก่อนลงมือ

```js
seraph: {
  day: 3,                    // 1..5
  cycleRound: 2,             // รอบที่เท่าไหร่ (ใช้เลือกโทนสี + เพลงวันที่ 5)
  phase: "draw",             // "draw" | "reward" | "place" | "placeResult" | "duelQueue" | "duel"
  place: null,               // สถานที่ที่ผู้ชมคนนี้เลือก (ของตัวเองเท่านั้น — ห้ามส่งของคนอื่น)
  placedCount: 4,            // จำนวนคนที่เลือกสถานที่แล้ว (ไม่บอกว่าเลือกอะไร)

  matrix: { held: 2, placed: { "<playerId>": 3 } },   // ของผู้ชมคนนี้เท่านั้น
  watchedBy: 1,              // จำนวนคนที่ลง Matrix ระดับ 3 ใส่เรา (ไม่บอกว่าใคร)

  skillLevel: 2,
  capacity: { hp: 3, armor: 2, skill: 4 },

  pairs: [ { a: "<pid>", b: "<pid>" } ],   // ประกาศตั้งแต่จบวันที่ 2
  bye: "<pid>" | null,
  myOpponent: "<pid>" | null,

  duelQueue: [ 0, 1, 2 ],    // ลำดับคู่
  duelIndex: 0,              // คู่ที่กำลังลงสนาม
  revealed: [ "<pid>", ... ] // ตัวละครที่ผู้ชมคนนี้เคยเห็นแล้ว — คุมฉาก S8c
}
```

**จุดที่ต้องระวังเป็นพิเศษ**
1. `revealed` เป็น **per-viewer** — คนละคนได้คนละลิสต์ ต้องคิดใน `buildStateFor` ไม่ใช่ broadcast รวม
2. `matrix.placed` ส่งเฉพาะของผู้ชมคนนั้น — ถ้าเผลอส่งทั้งก้อน ระบบสืบสวนพังทันที
3. `watchedBy` ส่งแค่ **จำนวน** ห้ามส่ง id (ดู S9)
4. ฉาก S8c ใช้ `queueCutscene(p, key, onlyFor)` โดย `onlyFor` = คนที่ยังไม่เคยเห็น — คนที่เหลือได้ `cutscene: null` แล้ววาดกระดานปกติ **แต่ยังนับเวลาเท่ากัน** (patch 2.8.1 รองรับอยู่แล้ว)
5. `lastLog` ยังเป็นรูรั่วตัวตนตามสเปกกติกา §14 ข้อ 2 — ฉากพวกนี้ไม่ช่วยอุด ต้องกรองที่ต้นทางแยกต่างหาก

---

## 9. ไฟล์สื่อที่มีจริง + สิ่งที่ทำด้วย CSS/SVG

### ไฟล์ที่มีแล้วและถูกใช้จริงในโค้ด

| ไฟล์ | ขนาดจริง | ใช้ที่ | หมายเหตุ |
|---|---|---|---|
| `update/background_normal.gif` | 500×281 · 5.2 MB | S1–S3 กระดานวันที่ 1–4 · ฉากหลัง S0 | |
| `update/background_rest.gif` | 358×200 · 2.8 MB | S4 เลือกสถานที่ · S5 ฉากในสถานที่ | |
| `update/background_day_5.gif` | 500×281 · 7.1 MB | S7–S10 **รอบกลางวัน** | |
| `update/background_night_5.gif` | 500×281 · 2.8 MB | S7–S10 **รอบกลางคืน** | เข้าคู่กับวงจรวัน/คืนใน §6 พอดี |
| `update/glitch_screen.gif` | 500×500 · 0.77 MB | ทุกกลิตช์ (`GlitchCut`) | blend `screen` opacity .62 |
| `ห้องพัก / โบสถ์ / สวนสาธารณะ / ห้องสมุด / ร้านสะดวกซื้อ.jpg` | – | S4 การ์ดสถานที่ · S5 ฉากหลังในสถานที่ | ชื่อไฟล์ไทย ต้องผ่าน `encodeURI` (ทำแล้วใน `assets.js`) |
| `theme/day1-4` · `theme/rest_time` · `day5theme/*` · `sond_effect/*` | – | ตาม §6 | ผูกเข้า `audio.js` แล้วเป็นคีย์ `sc_*` |

### 2 ข้อจำกัดของไฟล์ชุดนี้ และวิธีที่โค้ดรับมือ

**1) ความละเอียดต่ำ — 500×281 ต้องขยายเป็น 1920 (ขยาย 3.8 เท่า) จึงเบลอแน่นอน**
แทนที่จะสู้กับความเบลอ โค้ดเลือก **ทำให้มันเป็นสไตล์** ด้วยชั้นเคลือบ 5 ชั้นใน `SeraphBackground`:
GIF → เคลือบสีช่วงเวลา → ตารางข้อมูล → `sc-scanlines` (เส้นสแกน + แถบ CRT กวาดลง) → `sc-noise` → วีเนต
ผลคือฉากหลังอ่านเป็น "ฟีดดิจิทัลความละเอียดต่ำของ SE.RA.PH" ซึ่งเป็นภาษาของโหมดนี้อยู่แล้ว
และเพราะ UI ที่วางทับด้านบนคมชัด ความต่างจึงอ่านเป็น **ระยะชัดลึก** ไม่ใช่ภาพแตก

**2) น้ำหนักรวม 18.7 MB — มากเกินกว่าจะโหลดพร้อมกัน**
`assets.js` แบ่งเป็น 3 ระลอกตามลำดับการใช้จริง:

| ระลอก | เรียกตอน | โหลดอะไร | ขนาด |
|---|---|---|---|
| 1 | เข้าโหมด | `background_normal` + `glitch_screen` | ~6.0 MB |
| 2 | จบการจั่วไพ่วันแรก | `background_rest` + ภาพสถานที่ 5 ใบ | ~3.9 MB |
| 3 | เข้าวันที่ 4 | ฉากหลังวันดวล **เฉพาะช่วงเวลาที่รอบนี้ใช้** | 2.8 **หรือ** 7.1 MB |

> **ข้อเสนอที่คุ้มที่สุดถ้าจะทำต่อ:** แปลง GIF 4 ไฟล์เป็น **WebM (VP9) หรือ MP4** แล้วเปลี่ยน `<img>` เป็น `<video autoplay muted loop playsinline>`
> จากประสบการณ์ทั่วไปจะเล็กลงราว 85–95% (18.7 MB → ~1–3 MB) **และ** ใส่ความละเอียด 1280–1920 ได้ในขนาดที่ยังเล็กกว่า GIF เดิมมาก
> แก้จุดเดียวคือ `SeraphBackground` ใน `ui.jsx` — โครงชั้นเคลือบทั้งหมดใช้ต่อได้เลย

### สิ่งที่ทำด้วย CSS/SVG ทั้งหมดแล้ว (ไม่ต้องหาไฟล์เพิ่ม)

ตารางข้อมูลเปอร์สเปกทีฟ 3 ระดับ · กลิตช์ (สไลซ์ + RGB split + แฟลช) · จอแตกเป็นเสี่ยง 16 ชิ้น (SVG polygon) ·
สลายเป็นบล็อกพิกเซล 14 แถบ · เงาดำ `???` · แถบวัน 5 จุดพร้อมดาบไขว้ที่เต้นตลอด · ผังคู่ดวล + ป้าย VS ·
เรดาร์ Matrix (SVG: วง ping + เข็มกวาด + เส้นข้อมูล) · ลูกบาศก์ข้อมูล 3 มิติ 6 หน้า · หลอดค่าที่วาดตัวเองกลับมา ·
ช่องความจุที่งอกใหม่ · บันไดระดับทักษะ 6 ขั้น · การ์ดสกิลพลิกเข้าจอ · ข้อความระบบพิมพ์ทีละตัว · เศษข้อมูล ·
กรอบแดง "ถูกจับตา" · คลื่นดาเมจ · toast/banner

### ที่ยังอยากได้ (ไม่มีก็เล่นได้ — โค้ดมีทางถอยไว้หมดแล้ว)

| # | ไฟล์ | ทำไม | ถ้าไม่มีตอนนี้ใช้อะไรแทน |
|---|---|---|---|
| 1 | `mooncell/sond_effect/duel_call.mp3` | เสียงกระแทกตอน `VS` ชนกลางจอ (S6/S8) | `cut_glit` |
| 2 | `mooncell/sond_effect/delete.mp3` | เสียงตอนผู้เล่นถูกลบ (S10) | `cut_glit` ยืด 1.4s |
| 3 | `mooncell/sond_effect/reveal.mp3` | เสียงสแกนไล่ขึ้นตอนเปิดเผยตัวละคร (S8c) | `cut_glit` + `noti2` |
| 4 | `mooncell/theme/pairing.mp3` | เพลงสั้นฉากประกาศคู่ดวล 7–10 วิ | ความเงียบ + `noti2` (ได้อารมณ์ไปอีกแบบ) |
| 5 | `mooncell/theme/victory.mp3` | เพลงผู้ชนะคนสุดท้าย (S12) | `main_home.mp3` เดิม |
| 6 | `mooncell/ui/logo_seraph.png` | โลโก้พื้นโปร่งสำหรับ S0 | ตัวอักษร `SE.RA.PH` + `.glitch-p` (ใช้อยู่ตอนนี้) |

---

## 10. ที่ยังไม่ได้เคาะ (ต่อจาก §15 ของสเปกกติกา)

| # | เรื่อง | ทำไมต้องเคาะก่อนเขียนโค้ด |
|---|---|---|
| 1 | ~~เข้าโหมดนี้ยังไง~~ **เคาะแล้ว: `gameMode: "seraph"` ในหน้าโหวตโหมดเดิม** | โหวตผ่าน `selectGameMode {mode}` เหมือน ffa/duo/trio/overload · `startMatch()` แตกทางไปลูป 5 วันเมื่อ mode = seraph · ไม่ต้องผ่าน `TEAM_SETUP` |
| 2 | **เวลาเลือกสถานที่** ตั้งไว้ 20 วิในสเปกนี้ — ยาวไปหรือสั้นไป? | กระทบงบเวลาต่อวันทั้งหมด |
| 3 | **ผู้เล่นที่ตกรอบแล้วดูอะไรได้บ้าง** — ดูได้ทุกคู่ หรือดูได้แค่ที่ยังไม่รู้ตัวตน? | กระทบ `revealed` per-viewer และความสมดุลของข้อมูล |
| 4 | **ฉากเปิดเผยตัวละคร ใช้ภาพนิ่งหรือวีดีโอ** — เอนจินรองรับวีดีโอผ่าน `TRANSFORMS` อยู่แล้ว | ถ้าใช้วีดีโอ ต้องมี 43 คลิป ซึ่งไม่สมจริง — สเปกนี้จึงออกแบบเป็นภาพนิ่ง + เอฟเฟกต์ |
| 5 | ราคาของ/จำนวนช่องร้านค้า (สเปกกติกา §15.1) | กระทบการ์ดร้านสะดวกซื้อใน S4/S5.5 |
| 6 | เกราะฟื้น +1 ทุกเทิร์นคู่ ใช้ในวันที่ 5 ไหม (สเปกกติกา §15.2) | กระทบว่าต้องมีอนิเมชันเกราะฟื้นระหว่างดวลหรือไม่ |
